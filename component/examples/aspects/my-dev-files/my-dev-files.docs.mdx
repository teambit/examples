---
description: An aspect that registers a glob pattern for dev files
labels: ['demo']
---

Add this aspect to your workspace config, to apply it (and its dev files pattern) on all components.

```json
{
  "company.scope/extensions/my-dev-files": {}
}
```
